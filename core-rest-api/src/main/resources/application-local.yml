spring:
  datasource:
    url: jdbc:mysql://101.35.219.227:3306/ruiyun-hwy?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: sc2022_~*
    druid:
      initial-size: 5 #连接池初始化大小
      min-idle: 10 #最小空闲连接数
      max-active: 20 #最大连接数
      web-stat-filter:
        exclusions: "*.js,*.gif,*.jpg,*.png,*.css,*.ico,/druid/*" #不统计这些请求数据
      stat-view-servlet: #访问监控网页的登录用户名和密码
        login-username: druid
        login-password: druid

rocketmq:
  name-server: 124.223.114.215:9876
  producer:
    group: async_crawl_group
    topic: ASYNC_CRAWL_TOPIC
    send-message-timeout: 3000
    mqMax: 32
  maxCrawlTime: 1800000 #一次爬取最大30分钟
  maxCrawlCount: 3 #一台机器最多执行3个爬取请求,因为每个请求中最多可能有10个网站的爬取线程

crawl:
  target:
    - crawlServiceFroOneImpl
    - crawlServiceFroCmaImpl
    - crawlServiceFroMskImpl

mybatis:
  mapper-locations:
    - classpath*:dao/*.xml
    - classpath*:com/**/mapper/*.xml
  configuration:
    call-setters-on-nulls: true
management: #开启SpringBoot Admin的监控
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

feign:
  okhttp:
    enabled: true
ribbon:
  ConnectTimeout: 300000 #服务请求连接超时时间（毫秒）
  ReadTimeout: 300000 #服务请求处理超时时间（毫秒）

logging:
  level:
    root: debug
    framework: debug